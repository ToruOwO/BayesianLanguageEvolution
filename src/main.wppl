let generate_transition = function (alpha, m, eps) {
    let matrix = transition_matrix(alpha, m, eps);
    let filename = "output/" + alpha +"_"+ m + "_" + eps + ".json";
    json.write(filename,matrix);
    console.log(filename);
};